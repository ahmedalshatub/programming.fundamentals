#include<cstring>
#include <iostream>
using namespace std;
int main() {
char str[200];
cin.getline(str,200);
int count = 0;




for (int i = 0;str[i] != '\0';i++)

{

    if (str[i] == ' ')

        count++;

}
cout<<"the number of word in the string is : "<<count+1<<endl;
return 0;
}
